name: CRD_macOS_Remote_Access

on:
  workflow_dispatch:

jobs:
  setup-macos-crd:
    runs-on: macos-latest
    timeout-minutes: 2000

    steps:
      - name: 🍏 System Info
        run: |
          echo "=== 🍎 macOS Environment Info ==="
          sw_vers
          uname -a
          echo "================================="

      - name: 📦 Install Chrome Remote Desktop (Fix Mount Path)
        run: |
          set -e
          echo "📦 Downloading Chrome Remote Desktop DMG..."
          curl -L -o /tmp/chromeremotedesktop.dmg "https://dl.google.com/dl/chrome-remote-desktop/chromeremotedesktop.dmg"

          echo "💿 Mounting DMG..."
          # Gunakan hdiutil JSON output agar aman untuk nama volume dengan spasi
          MOUNT_POINT=$(hdiutil attach /tmp/chromeremotedesktop.dmg -nobrowse -plist | \
            plutil -extract system-entities xml1 -o - - | \
            grep -A1 '<key>mount-point</key>' | grep '<string>' | sed 's/.*<string>\(.*\)<\/string>.*/\1/' | head -n 1)
          
          if [ -z "$MOUNT_POINT" ]; then
            echo "❌ Gagal mendapatkan mount point."
            exit 1
          fi

          echo "📀 Mounted at: $MOUNT_POINT"
          ls "$MOUNT_POINT" || echo "⚠️ Tidak bisa membaca isi mount, lanjut deteksi manual..."

          # Deteksi .pkg atau .app
          if ls "$MOUNT_POINT"/*.pkg 1>/dev/null 2>&1; then
            PKG=$(ls "$MOUNT_POINT"/*.pkg | head -n 1)
            echo "📦 Found PKG installer: $PKG"
            sudo installer -pkg "$PKG" -target /
          elif find "$MOUNT_POINT" -maxdepth 1 -name "*.app" | grep -q .; then
            APP_PATH=$(find "$MOUNT_POINT" -maxdepth 1 -name "*.app" | head -n 1)
            echo "📦 Found App bundle: $APP_PATH"
            sudo cp -R "$APP_PATH" /Applications/
          else
            echo "❌ Tidak ditemukan file .pkg atau .app di dalam DMG!"
            echo "Isi volume:"
            ls -R "$MOUNT_POINT"
            exit 1
          fi

          echo "💾 Unmounting..."
          hdiutil detach "$MOUNT_POINT"
          rm /tmp/chromeremotedesktop.dmg
          echo "✅ Chrome Remote Desktop installed successfully!"

      - name: 👤 Create Remote User
        run: |
          USERNAME="remoteuser"
          PASSWORD=$(openssl rand -base64 10)
          sudo sysadminctl -addUser "$USERNAME" -password "$PASSWORD"
          sudo dscl . -append /Groups/admin GroupMembership "$USERNAME"
          echo "CRD_USER=$USERNAME" >> $GITHUB_ENV
          echo "CRD_PASS=$PASSWORD" >> $GITHUB_ENV
          echo "✅ Created user $USERNAME / $PASSWORD"

      - name: 🧠 Setup Chrome Remote Desktop
        env:
          CRD_AUTH_CODE: ${{ secrets.CRD_AUTH_CODE }}
        run: |
          if [ -z "$CRD_AUTH_CODE" ]; then
            echo "❌ Missing CRD_AUTH_CODE secret!"
            echo "Generate it from: https://remotedesktop.google.com/headless"
            exit 1
          fi

          PIN=$(jot -r 1 100000 999999)
          echo "CRD_PIN=$PIN" >> $GITHUB_ENV

          HOST_PATH="/Applications/Chrome Remote Desktop Host.app/Contents/MacOS/Chrome Remote Desktop Host"
          ALT_PATH="/Applications/Chrome Remote Desktop.app/Contents/MacOS/Chrome Remote Desktop Host"

          if [ -f "$HOST_PATH" ]; then
            BIN="$HOST_PATH"
          elif [ -f "$ALT_PATH" ]; then
            BIN="$ALT_PATH"
          else
            echo "❌ CRD Host binary not found!"
            ls -R /Applications | grep 'Chrome'
            exit 1
          fi

          echo "🚀 Registering CRD host..."
          sudo "$BIN" \
            --code="$CRD_AUTH_CODE" \
            --redirect-url="https://remotedesktop.google.com/_/oauthredirect" \
            --name="GitHub-macOS-${{ github.run_id }}" \
            --pin="$PIN" &
          echo "✅ CRD link initiated! PIN: $PIN"

      - name: 📋 Connection Info
        run: |
          echo "=============================================="
          echo "🍏 Chrome Remote Desktop for macOS Ready!"
          echo "User     : $CRD_USER"
          echo "Password : $CRD_PASS"
          echo "PIN      : $CRD_PIN"
          echo "Access   : https://remotedesktop.google.com/access"
          echo "=============================================="

      - name: 💤 Keep Alive
        run: |
          while true; do
            echo "[$(date)] 🟢 macOS CRD session active..."
            sleep 300
          done
