name: VNC_TIPSUNIX_DEBUG

on:
  workflow_dispatch:

jobs:
  secure-vnc:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      # Langkah ini dimodifikasi untuk mencari file
      - name: Install and Configure TightVNC
        run: |
          # 1. Install TightVNC
          $vncUrl = "https://www.tightvnc.com/download/2.8.81/tightvnc-2.8.81-gpl-setup-64bit.msi"
          $installerPath = "$env:TEMP\tightvnc.msi"
          Invoke-WebRequest -Uri $vncUrl -OutFile $installerPath

          Start-Process msiexec.exe -ArgumentList @(
              "/i", "`"$installerPath`"",
              "/quiet", "/norestart",
              "ADDLOCAL=Server",
              "SERVER_REGISTER_AS_SERVICE=1",
              "SERVER_START_SERVICE=1"
          ) -Wait

          Remove-Item $installerPath -Force
          Write-Host "Install complete. Now searching for files..."
          Start-Sleep -Seconds 5

          # 2. DEBUG: List Program Files directories
          Write-Host "--- Listing C:\Program Files ---"
          ls "C:\Program Files"
          Write-Host "--------------------------------"
              
          Write-Host "--- Listing C:\Program Files (x86) ---"
          ls "C:\Program Files (x86)"
          Write-Host "------------------------------------"

          # 3. DEBUG: Search for the file
          Write-Host "--- Searching for tvnpassword.exe on C: drive (this may take a minute) ---"
          $filePath = Get-ChildItem -Path "C:\" -Filter "tvnpassword.exe" -Recurse -ErrorAction SilentlyContinue | Select-Object -ExpandProperty FullName

          if ($filePath) {
              Write-Host "SUCCESS! File found at: $filePath"
          } else {
              Write-Error "File tvnpassword.exe NOT FOUND anywhere on C: drive."
          }
          Write-Host "-----------------------------------------------------------------------"
              
          Write-Host "Please copy the output of this step and paste it in the chat."
          Write-Host "The workflow will now intentionally fail so we can read the log."
          exit 1
      
      # Langkah-langkah di bawah ini tidak akan berjalan,
      # karena langkah di atas akan sengaja gagal.
      - name: Restart VNC Service
        run: |
          Write-Host "Restarting TightVNC service to apply password..."
          Stop-Service -Name "TightVNC Server" -ErrorAction SilentlyContinue -Force
          Start-Sleep -Seconds 3
          Start-Service -Name "TightVNC Server"
          Write-Host "VNC Service restarted successfully."

      - name: Configure VNC Firewall
        run: |
          netsh advfirewall firewall delete rule name="VNC-Tailscale"
          netsh advfirewall firewall add rule name="VNC-Tailscale" `
            dir=in action=allow protocol=TCP localport=5900
          Write-Host "Firewall port 5900 opened."

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
            $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
            Start-Sleep -Seconds 5
            $retries++
          }
          
          if (-not $tsIP) {
            Write-Error "Tailscale IP not assigned. Exiting."
            exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Verify VNC Accessibility
        run: |
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 5900
          if (-not $testResult.TcpTestSucceeded) {
            Write-Error "TCP connection to VNC port 5900 failed"
            exit 1
          }
          Write-Host "TCP connectivity successful!"

      - name: Maintain Connection
        run: |
          Write-Host "`n=== VNC ACCESS ==="
          Write-Host "http://www.youtube.com/@TipsUNIX"
          Write-Host "Address: ${env:TAILSCALE_IP}:5900"
          Write-Host "Password: $env:VNC_PASS"
          Write-Host "==================`n"
          
          while ($true) {
            Write-Host "[$(Get-Date)] VNC Active - Use Ctrl+C in workflow to terminate"
            Start-Sleep -Seconds 300
          }
