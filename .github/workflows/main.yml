name: VNC_TIPSUNIX_UltraVNC

on:
  workflow_dispatch:

jobs:
  secure-vnc:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      - name: Install and Configure UltraVNC
        run: |
          # 1. Generate a random password for VNC
          $charPool = ([char[]](48..57) + [char[]](65..90) + [char[]](97..122) + '!', '@', '#', '$', '%', '&', '*')
          $password = -join ($charPool | Get-Random -Count 12)
          echo "VNC_PASS=$password" >> $env:GITHUB_ENV

          # 2. Install UltraVNC using Chocolatey
          Write-Host "Installing UltraVNC using Chocolatey..."
          choco install ultravnc -y --no-progress
          Write-Host "UltraVNC installed. Now setting password..."

          # 3. Atur password secara paksa menggunakan uvnc_settings.exe
          # Path default UltraVNC
          $vncUtil = "${env:ProgramFiles}\uvnc bvba\UltraVNC\uvnc_settings.exe"
          
          if (-not (Test-Path $vncUtil)) {
            Write-Error "uvnc_settings.exe not found at $vncUtil. Installation may have failed."
            exit 1
          }

          # Gunakan parameter UltraVNC untuk mengatur password
          & $vncUtil -setpassword $password
          & $vncUtil -setviewonlypassword $password

          Write-Host "VNC password has been set via utility."

      - name: Restart VNC Service
        run: |
          Write-Host "Restarting UltraVNC service to apply password..."
          # Nama layanan untuk UltraVNC adalah 'uvnc_service'
          Stop-Service -Name "uvnc_service" -ErrorAction SilentlyContinue -Force
          Start-Sleep -Seconds 3
          Start-Service -Name "uvnc_service"
          Write-Host "VNC Service restarted successfully."

      - name: Configure VNC Firewall
        run: |
          # Port VNC sama (5900)
          netsh advfirewall firewall delete rule name="VNC-Tailscale"
          netsh advfirewall firewall add rule name="VNC-Tailscale" `
            dir=in action=allow protocol=TCP localport=5900
          Write-Host "Firewall port 5900 opened."

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
            $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
            Start-Sleep -Seconds 5
            $retries++
          }
          
          if (-not $tsIP) {
            Write-Error "Tailscale IP not assigned. Exiting."
            exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Verify VNC Accessibility
        run: |
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 5900
          if (-not $testResult.TcpTestSucceeded) {
            Write-Error "TCP connection to VNC port 5900 failed"
            exit 1
          }
          Write-Host "TCP connectivity successful!"

      - name: Maintain Connection
        run: |
          Write-Host "`n=== VNC ACCESS ==="
          Write-Host "http://www.youtube.com/@TipsUNIX"
          Write-Host "Address: ${env:TAILSCALE_IP}:5900"
          Write-Host "Password: $env:VNC_PASS"
          Write-Host "==================`n"
          
          while ($true) {
            Write-Host "[$(Get-Date)] VNC Active - Use Ctrl+C in workflow to terminate"
            Start-Sleep -Seconds 300
          }
